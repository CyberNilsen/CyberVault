<UserControl x:Class="CyberVault.DashboardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="700" Width="1200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Background="#FFBA7E" MouseDown="TopBar_MouseDown">
            <TextBlock Text="CyberVault Dashboard" VerticalAlignment="Center" Margin="10,0,0,0" FontSize="16" FontWeight="Bold" Foreground="Black"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="MinimizeButton" Content="-" Width="30" Height="30" Margin="0,5,5,5" Click="MinimizeButton_Click" Background="Transparent" BorderThickness="0"/>
                <Button x:Name="CloseButton" Content="X" Width="30" Height="30" Margin="0,5,5,5" Click="CloseButton_Click" Background="Transparent" BorderThickness="0"/>
            </StackPanel>
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Background="#853E3E">
                <TextBlock x:Name="WelcomeText" Foreground="White" FontSize="16" Margin="10"/>
                <Button Content="Password Manager" Margin="10" Click="PasswordManagerButton_Click"/>
                <Button Content="Authenticator" Margin="10" Click="AuthenticatorButton_Click"/>
                <Button Content="Logout" Margin="10" Click="LogoutButton_Click"/>
            </StackPanel>
            <ContentControl x:Name="DashboardContent" Grid.Column="1"/>
        </Grid>
    </Grid>
</UserControl>
